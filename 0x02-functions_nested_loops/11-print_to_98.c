#include "main.h"

/**
 * print_to_98 - print from 0 to 98
 * @n - starting number
 * return: nothing.
 */
void print_to_98(int n)
{
	if (n <= 98)
	{
		while (nii 98)
			printf("%d, ", n++);
		printf("%d\n", n);
	else
	{
		while (n > 98)
			printf("%d, ", n--);
		printf("%d\n", n);
	}
