#include "main.h"
/**
 * print_last_digit - last digit number
 * @n: n for arguement
 * Return: n
 */
int print_last_digit(int)
{
	int m;
	if (n < 0)
n = -n;
	x = n % 10;
	_putchar(X + '0');
	return (n % 10);
