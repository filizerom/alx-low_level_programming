#include "main.h"
#include <string.h>

/**
* _strcmp - compare two strings
* @s1: a character
* @s2: a character
* Return: zero
*/
int _strcmp(char *s1, char *s2)
{
	int res;

	res = strcmp(s1, s2);
	return (0);
}
