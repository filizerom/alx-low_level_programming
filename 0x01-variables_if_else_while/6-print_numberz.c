#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - print all digit number of 10 starting from zero
 * Description: use main function and putchar only
 *Return: 0
 */
int main(void)
{
int i;
for (i = 0; i < 10; i++)
{
	putchar(i % 10) + 0));
}
putchar("\n");
return (0);
}
